{
  "name": "chat",
  "version": "1.0.0",
  "description": "Chat experiment with web sockets",
  "main": "index.js",
  "scripts": {
    "start": "npm install && node index.js",
    "start-client": "cd client && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "http": "0.0.1-security",
    "path": "^0.12.7",
    "socket.io": "^4.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
